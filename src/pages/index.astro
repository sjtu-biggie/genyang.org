---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Gen Yang - Personal Portfolio" description="Software Engineer & Quantitative Trading Enthusiast" image="/gen-yang-profile.jpg">
  <div class="index-content framed">
    <div class="intro-header">
      <div class="intro-text">
        <h1 id="hello-there">$ whoami</h1>
        <p>Welcome to my digital workspace. I'm Gen Yang, a software engineer with a passion for building high-performance systems and exploring quantitative finance.</p>
      </div>
      <div class="intro-avatar">
        <img src="/gen-yang-profile.jpg" alt="Gen Yang" />
      </div>
    </div>
    
    <div class="terminal-block">
      <h2>$ ls -la ~/skills</h2>
      <ul class="skill-list">
        <li><strong>Languages:</strong> Python, C++, SQL, R</li>
        <li><strong>Analytics:</strong> Machine Learning, Statistical Modeling, Quantitative Analysis</li>
        <li><strong>Finance:</strong> CFA Level I, Algorithmic Trading, Risk Management</li>
        <li><strong>Systems:</strong> High-performance data processing, Database optimization</li>
      </ul>
    </div>

    <div class="terminal-block">
      <h2>$ cat ~/current_projects.txt</h2>
      <p>Currently working on building a comprehensive trading system with real-time monitoring capabilities. This portfolio site serves as both a professional showcase and a control center for my quantitative trading experiments.</p>
      
      <div class="terminal-interactive">
        <h3>$ uptime</h3>
        <p id="uptime-display">Loading system uptime...</p>
      </div>
    </div>

    <div class="navigation-links">
      <h2>$ find . -name "*.page"</h2>
      <ul class="link-list">
        <li><a href="/about">./about.page</a> - More about my background and experience</li>
        <li><a href="/projects">./projects/</a> - Portfolio of completed and ongoing projects</li>
        <li><a href="/gallery">./gallery.page</a> - Sports and recreational activities</li>
      </ul>
    </div>
  </div>
</BaseLayout>

<style>
  .terminal-block {
    margin: 2rem 0;
    padding: 1rem;
    border: 1px solid var(--accent);
    border-radius: 4px;
  }
  
  .skill-list, .link-list {
    list-style: none;
    padding: 0;
  }
  
  .skill-list li, .link-list li {
    margin: 0.5rem 0;
    padding-left: 1rem;
  }
  
  .skill-list li::before {
    content: "├─ ";
    color: var(--accent);
    font-weight: bold;
    margin-left: -1rem;
    margin-right: 0.5rem;
  }
  
  .link-list li::before {
    content: "└── ";
    color: var(--accent);
    font-weight: bold;
    margin-left: -1rem;
    margin-right: 0.5rem;
  }
  
  .navigation-links {
    margin-top: 2rem;
  }
  
  .terminal-interactive {
    margin-top: 1rem;
    padding: 1rem;
    background: rgba(var(--accent-rgb), 0.1);
    border-radius: 4px;
    font-family: 'Fira Code', monospace;
  }
  
  .terminal-interactive h3 {
    margin: 0 0 0.5rem 0;
    color: var(--accent);
    font-size: 1rem;
  }
  
  #uptime-display {
    color: var(--foreground);
    font-size: 0.9rem;
    margin: 0;
  }
  
  .intro-header {
    display: grid;
    gap: 2rem;
    align-items: center;
    margin-bottom: 2rem;
  }
  
  @media (min-width: 768px) {
    .intro-header {
      grid-template-columns: 1fr auto;
    }
  }
  
  .intro-avatar {
    display: flex;
    justify-content: center;
  }
  
  @media (min-width: 768px) {
    .intro-avatar {
      justify-content: flex-end;
    }
  }
  
  .intro-avatar img {
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    border: 3px solid var(--accent);
    box-shadow: 0 4px 12px rgba(var(--accent-rgb), 0.2);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .intro-avatar img:hover {
    transform: scale(1.1) rotate(5deg);
    box-shadow: 0 6px 20px rgba(var(--accent-rgb), 0.4);
  }
</style>

<script>
  // Dynamic uptime display
  function updateUptime() {
    const startDate = new Date('2025-01-01'); // Website launch date
    const now = new Date();
    const diffMs = now - startDate;
    
    const days = Math.floor(diffMs / (1000 * 60 * 60 * 24));
    const hours = Math.floor((diffMs % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
    const minutes = Math.floor((diffMs % (1000 * 60 * 60)) / (1000 * 60));
    
    const uptimeElement = document.getElementById('uptime-display');
    if (uptimeElement) {
      uptimeElement.textContent = `genyang.org has been running for ${days} days, ${hours} hours, ${minutes} minutes`;
    }
  }
  
  // Update uptime when page loads
  document.addEventListener('DOMContentLoaded', updateUptime);
  
  // Update uptime every minute
  setInterval(updateUptime, 60000);
</script>